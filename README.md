# GiftShare — Community Gifting Board

GiftShare е уеб приложение тип "общностна дъска за подаръци", в което регистрирани потребители могат да публикуват вещи, които подаряват безплатно. Други потребители могат да разглеждат публикуваните обяви и да взаимодействат с тях чрез гласуване и коментиране.

## Технологии

- **Laravel 12** - PHP фреймуърк
- **Livewire 3** - За динамични интерфейси без излишен JavaScript
- **Bootstrap 5** - За модерен и респонсивен UI
- **MariaDB/MySQL** - Релационна база данни
- **PHP 8.2+** - Програмен език

## Изисквания за средата

- PHP 8.2 или по-нова версия
- Composer
- MySQL/MariaDB
- Node.js и npm (за компилиране на assets при нужда)

Проектът е тестван на XAMPP с MariaDB.

## Стъпки за инсталация и стартиране

### 1. Клониране на проекта

```bash
cd C:\xampp\htdocs\myProject\CommunityGiftingBoard
```

### 2. Инсталиране на зависимостите

```bash
composer install
```

### 3. Конфигуриране на базата данни

Създайте файл `.env` от `.env.example` (ако все още не съществува) и настройте следните параметри:

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=community_gifting_board
DB_USERNAME=root
DB_PASSWORD=

APP_URL=http://127.0.0.1:8080
```

**Важно:** Уверете се, че базата данни `community_gifting_board` е създадена в MariaDB/MySQL преди изпълнение на миграциите.

### 4. Генериране на приложен ключ

```bash
php artisan key:generate
```

### 5. Изпълнение на миграции и seed данни

```bash
php artisan migrate --seed
```

Това ще създаде всички необходими таблици и ще попълни базата с демо данни:
- 10 категории
- 21 потребителя (включително тестов акаунт)
- 80 обяви
- Коментари и гласове за различни обяви

### 6. Създаване на симлинк за storage

```bash
php artisan storage:link
```

Това създава символен линк от `public/storage` към `storage/app/public`, за да могат качените снимки да бъдат достъпни през уеб сървъра.

### 7. Стартиране на локалния сървър

```bash
php artisan serve --port=8080
```

След това отворете браузъра и навигирайте до: **http://127.0.0.1:8080**

**Забележка:** Портът 8080 е използван, защото стандартният порт 8000 може да е зает. Ако искате да използвате друг порт, променете `--port` параметъра и актуализирайте `APP_URL` в `.env` файла.

## Демо потребители

След изпълнение на seed данните, можете да влезете с тестов акаунт:

- **Email:** `admin@test.com`
- **Парола:** `test_password123!`

Или можете да се регистрирате с нов акаунт през интерфейса.

## Указания за ползване

### Регистрация и вход

Приложението използва стандартна email/password автентификация чрез Laravel Breeze.

**Регистрация:**
1. Отворете началната страница (автоматично пренасочва към `/login`)
2. Кликнете на "Нямаш акаунт? Регистрирай се" или навигирайте към `/register`
3. Попълнете формата:
   - **Име** (минимум 3 символа, максимум 100)
   - **Имейл** (валиден, уникален)
   - **Парола** (минимум 8 символа)
   - **Потвърждение на парола**
4. След успешна регистрация:
   - Потребителят автоматично се логва
   - Пренасочва се към основния feed (`/items`)

**Вход:**
1. Отворете `/login` страницата
2. Въведете имейл и парола
3. Опционално: маркирайте "Запомни ме" за по-дълга сесия
4. След успешен вход, пренасочва се към `/items`

**Важно:**
- Само регистрирани потребители имат достъп до съдържанието
- Гости, които опитат да достъпят защитени страници, се пренасочват автоматично към `/login`
- Логнати потребители, които опитат да достъпят `/register` или `/login`, се пренасочват към основната страница

### Забравена парола (Forgot Password) – Log mailer

В development среда приложението може да е настроено да **не изпраща реални имейли**, а да ги записва в лог (Laravel `log` mailer).

- **Къде се вижда reset линкът**: `storage/logs/laravel.log`
- **Какво да търсите**: `Reset Password:` или `Reset Password Notification`

За да гарантирате, че е активен log mailer, в `.env` използвайте:

```env
MAIL_MAILER=log
```

След промяна на `.env` изпълнете:

```bash
php artisan config:clear
```

### Разглеждане на обяви

- След вход ще видите списък с всички обяви.
- Показват се по **9 обяви на страница** (странициране).
- Използвайте филтрите горе за да филтрирате по:
  - **Категория** - падащо меню с всички категории
  - **Град** - текстово поле за търсене по град
  - **Статус** - налични или подарени обяви
- Използвайте полето за **търсене** за търсене по заглавие или описание.
- Изберете **сортиране** - най-нови или най-популярни обяви.

### Детайли на обява

- Кликнете на заглавие или бутон "Виж детайли" за да видите пълна информация за обявата.
- На детайлната страница можете да:
  - Разглеждате всички снимки (кликнете на снимка за пълен размер)
  - Гласувате с бутоните 👍 (upvote) или 👎 (downvote)
  - Добавяте коментари в секцията отдолу

### Създаване на обява

1. Кликнете на "Създай обява" в навигацията.
2. Попълнете формата:
   - **Заглавие** (задължително, 5-150 символа)
   - **Описание** (задължително, минимум 10 символа)
   - **Категория** (задължително)
   - **Град** (задължително)
   - **Тегло** и **Размери** (незадължителни)
   - **Снимки** (можете да качите няколко, максимум 2MB на файл)
3. Кликнете "Публикувай обява".

### Управление на обяви

- В навигацията кликнете на "Моите обяви" за да видите само вашите публикации.
- От там можете да:
  - Редактирате обява (бутон "Редактирай")
  - Маркирате обява като подарена/налична
  - Виждате детайли

- На детайлната страница на ваша обява ще видите допълнителни бутони за редактиране и маркиране на статуса.

### Коментари и гласуване

- Всеки логнат потребител може да коментира и гласува за обяви.
- Всеки потребител има право на един глас на обява (може да го промени или премахне).
- Коментарите се показват хронологично (най-стари първи).

## Структура на проекта

```
CommunityGiftingBoard/
├── app/
│   ├── Livewire/          # Livewire компоненти
│   │   ├── ItemList.php
│   │   ├── ItemShow.php
│   │   ├── CreateItem.php
│   │   ├── EditItem.php
│   │   ├── MyItems.php
│   │   ├── CommentsSection.php
│   │   └── VoteButtons.php
│   ├── Models/             # Eloquent модели
│   │   ├── Category.php
│   │   ├── Item.php
│   │   ├── Comment.php
│   │   ├── Vote.php
│   │   └── Image.php
│   └── Policies/           # Авторизационни политики
│       └── ItemPolicy.php
├── database/
│   ├── migrations/         # Миграции на базата данни
│   ├── factories/          # Фабрики за генериране на тестови данни
│   └── seeders/            # Seeders за попълване на данни
│       └── DatabaseSeeder.php
├── resources/
│   └── views/
│       ├── layouts/        # Blade layout файлове
│       │   ├── app.blade.php
│       │   ├── guest.blade.php
│       │   └── navigation.blade.php
│       ├── auth/           # Login / Register / Forgot password (Breeze)
│       ├── profile/        # Профил (Breeze)
│       ├── components/     # Blade components (Breeze)
│       └── livewire/       # Blade view файлове за Livewire компоненти
├── routes/
│   └── web.php             # Web маршрути
│   └── auth.php            # Auth маршрути (Breeze)
├── lang/bg/                # Преводи на български
├── lang/bg.json            # Преводи (JSON)
└── storage/
    └── app/
        └── public/         # Качени файлове (снимки)
```

## Функционалности

### Основни функции

- ✅ Регистрация и автентикация (Laravel Breeze)
- ✅ Публикуване на обяви с множество снимки
- ✅ Филтриране и търсене на обяви
- ✅ Сортиране по дата или популярност
- ✅ Гласуване (upvote/downvote) за обяви
- ✅ Коментиране на обяви
- ✅ Редактиране на собствени обяви
- ✅ Маркиране на обяви като подарени/налични
- ✅ Странициране на резултатите
- ✅ Респонсивен дизайн с Bootstrap 5

### Технически детайли

- **Валидация:** Сървърна валидация на всички форми с ясни съобщения за грешки
- **Авторизация:** Policy-базирана авторизация за защита на действията
- **Eager Loading:** Оптимизирани заявки с eager loading за избягване на N+1 проблеми
- **Файлово управление:** Използване на Laravel Storage API за качване на снимки
- **Динамично поведение:** Livewire компоненти за интерактивност без пълно презареждане

## Известни ограничения

- Не е имплементирано изтриване на обяви (може да се добави лесно)
- Премахването на снимки е налично **в страницата за редактиране на обява** (бутон **X** върху снимката) и изтрива както записа, така и файла от storage.
- Няма нотификации по имейл
- Няма система за съобщения между потребителите

## Разработка

Проектът следва Laravel best practices и PSR стандарти. Кодът е организиран, четим и подготвен за разширяване.

### Тестване

За да тествате функционалността:

1. Изпълнете миграциите и seed данните: `php artisan migrate:fresh --seed`
2. Влезте с тестовия акаунт: `admin@test.com` / `test_password123!`
3. Разгледайте обявите, филтрирайте, търсете
4. Създайте нова обява с качване на снимки
5. Коментирайте и гласувайте за обяви
6. Редактирайте собствена обява

## Лиценз

Този проект е създаден за образователни цели.

## Автор

Проектът е разработен като демонстрация на умения с Laravel, Livewire и Bootstrap.

---

**Забележка:** Това е тестов проект. За production използване се препоръчва допълнителна сигурност, оптимизация и тестване.
